# Copy this file to project ROOT as `.env` (same folder as index.js), NOT chatapp-client/.env.

MONGO_URI=
PORT=5001
CLIENT_URL=http://localhost:3000

# Plain-text secret used only for Guidelines room admin posting (socket verifyGuidelinesAdmin).
ADMIN_PASSWORD=
